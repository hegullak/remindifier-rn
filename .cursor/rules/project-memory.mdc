---
description: remindifier-rn project context — read memory files before exploring
alwaysApply: true
---

# remindifier-rn context

**Product name:** echoflow (UI). Repo/package/bundle still `remindifier-rn` until Phase C.

**Session start:** read `claude-current-remindifier-state.md` then `PROJECT_MEMORY.md`.  
**Session end:** update state file only when user requests **SESSION HANDOFF** — see `.cursor/rules/session-handoff.mdc`.

## Essentials (keep in sync with PROJECT_MEMORY.md)

- Expo 54 + expo-router; **Expo v56 docs** before coding (`AGENTS.md`)
- **All UI strings** via `useTranslation()` / `src/i18n/locales/{en,no}.ts` — code in English
- NO: **Merkedager**; EN: **Red-letter days**; section titles include “denne uken” / “this week”
- Tabs: `brief` | `gather` (events) | `people` | `myself`; settings at `/settings` (avatar); header **echoflow** mark top-left (`AppBrand`)
- Brief: greeting → date+uke → Dagen min → I morgen → Uken min; past events in dropdown; dev tomorrow stubs in `__DEV__`
- Local encrypted SQLite (Drizzle); Clerk = auth only
- Pass `locale` to `upcomingRedLetterDays`, `formatBriefDateLine`
- Biome + Jest; branch `sandbox`; don’t commit `.env` / logs / coverage
- Pre-merge locally: `npm run check`; CI on `sandbox` PRs (doc-only + `[skip ci]` skipped; Maestro local only)

## Git workflow (each agent session)

1. **Start:** `git pull --rebase origin sandbox`
2. **End:** commit and push **only when the user asks**

When unsure about structure or naming, open `PROJECT_MEMORY.md` instead of broad repo search.
